# Supabase (required)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Production: canonical site URL for OAuth redirects and metadata (recommended on Vercel Production)
# Example: https://riskopslab.com
# Preview deployments can omit this; the app uses VERCEL_URL at build time for callbacks.
# NEXT_PUBLIC_SITE_URL=
